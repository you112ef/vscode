# Cloudflare Pages npm configuration
# Skip native dependencies and scripts for web-only builds
ignore-scripts=true
optional=false
production=false

# Skip problematic native modules
# These modules require system libraries not available in Cloudflare Pages
ignore-engines=true
ignore-platform=true

# Use only web-compatible packages
prefer-offline=true
